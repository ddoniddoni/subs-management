# AGENTS.md

## 프로젝트 목표
이 저장소의 목표는 **Subscription Ops Console**이라는 제품을 만드는 것입니다.

이 프로젝트는 현실적인 구독 서비스 제품처럼 보여야 하며, 다음 두 가지 화면 영역으로 구성됩니다.

- **고객용 앱 (20~30%)**
  - 랜딩 페이지
  - 요금제 페이지
  - 회원가입 / 결제 목업
  - 내 구독 페이지
  - 결제 내역

- **운영자용 관리자 대시보드 (70~80%)**
  - 고객 관리
  - 구독 관리
  - 결제 관리
  - 환불 처리
  - 쿠폰 관리
  - 분석 대시보드
  - 관리자 권한
  - 감사 로그

핵심 목표는 현실적인 구독 서비스 운영 흐름을 갖춘, 유지보수 가능한 프론트엔드 제품을 만드는 것입니다.

## 제품 원칙
- 화려한 데모 화면보다 **실제 운영 업무에 가까운 흐름**을 우선합니다.
- 기능 개수를 늘리기보다 **안전한 관리자 워크플로우**를 더 중요하게 봅니다.
- 페이지마다 일회성으로 구현하기보다, **UI와 도메인 모듈을 재사용 가능하게 설계**합니다.
- 상태 전이, 확인 절차, 상태 배지, 변경 이력, 감사 가능성 등 **실제 서비스처럼 믿을 수 있는 흐름**을 만듭니다.
- 별도 요청이 없다면, 이 제품과 무관한 도메인이나 특이한 기능은 추가하지 않습니다.

## 기본 기술 방향
- 프로젝트를 처음부터 구성한다면 **Next.js (App Router) + TypeScript**를 사용합니다.
- 상호작용이 필요 없는 경우에는 **서버 컴포넌트**를 우선합니다.
- 브라우저 API 사용, 로컬 상태, 사용자 인터랙션이 필요한 경우에만 **클라이언트 컴포넌트**를 사용합니다.
- 패키지 매니저가 정해져 있지 않다면 **npm**을 우선합니다.
- 이미 lockfile이 있다면 기존 패키지 매니저를 따릅니다.
- 스타일링과 컴포넌트 시스템은 일관되게 유지합니다.
- 특별한 이유 없이 여러 UI 시스템을 섞지 않습니다.
- 초기에 실제 결제 연동에 의존하지 않도록, **명확한 API 경계와 목업/로컬 데이터 기반의 v1**을 우선합니다.

## 핵심 도메인
제품은 아래 엔티티를 중심으로 모델링합니다.

- customer
- plan
- subscription
- payment / invoice
- refund
- coupon / promotion
- admin user / role
- audit event
- dashboard metric snapshot

## 필수 화면 및 기능

### 고객용 영역
- 랜딩 페이지
- 요금제 / 플랜 페이지
- 체크아웃 또는 플랜 선택 흐름 (목업 가능)
- 내 구독 페이지
- 결제 / 청구 내역 페이지

### 관리자 대시보드 영역
- 고객 목록 및 상세 페이지
- 구독 상태 변경 흐름
- 결제 내역 및 결제 실패 처리
- 환불 승인 / 반려 흐름
- 쿠폰 발급 / 회수 흐름
- KPI 카드 및 차트
- 관리자 역할 / 권한 표시
- 감사 로그 / 이벤트 이력

## 권장 디렉터리 구조
저장소에 더 적절한 구조가 이미 없다면, 아래 구조를 기본으로 사용합니다.

- `src/app/` 또는 `app/`: 라우트와 레이아웃
- `src/features/`: 도메인 중심 기능 모듈
- `src/components/ui/`: 재사용 가능한 UI 기본 컴포넌트
- `src/components/shared/`: 공통 조합 컴포넌트
- `src/lib/`: 유틸리티, API 클라이언트, 상수, 헬퍼
- `src/types/`: 여러 도메인에서 공통으로 사용하는 타입
- `src/mocks/` 또는 `src/fixtures/`: 데모 / 시드 데이터
- `tests/`: 통합 테스트 / E2E 테스트
- `docs/`: 아키텍처 문서, 계획서, 트레이드오프 정리

## 엔지니어링 규칙
- **TypeScript strict mode**를 사용합니다.
- 특별한 이유가 없다면 `any`는 사용하지 않습니다.
- 컴포넌트는 작고, 역할이 명확하며, 조합 가능하게 유지합니다.
- 기능별 훅, 테스트, 타입은 가능하면 해당 기능 가까이에 둡니다.
- 재사용성을 높일 수 있다면 설정 기반 테이블/폼 구성을 우선합니다.
- 모든 비동기 화면은 반드시 아래 상태를 포함해야 합니다.
  - 로딩
  - 빈 상태
  - 에러
  - 성공
- 위험도가 높은 관리자 액션에는 반드시 아래가 포함되어야 합니다.
  - 확인 절차
  - 성공/실패 피드백
- 상태 문자열은 여기저기 하드코딩하지 말고 enum 또는 상수로 관리합니다.
- 에러를 조용히 무시하지 않습니다.
- 죽은 코드, 주석 처리된 코드, 의미 없는 TODO는 남기지 않습니다.

## UX 및 접근성 기준
- 관리자 테이블은 특별한 이유가 없다면 다음 기능을 지원해야 합니다.
  - 검색
  - 필터
  - 정렬
  - 페이지네이션
- 벌크 액션은 실제 워크플로우에 도움이 될 때만 추가합니다.
- 시맨틱 HTML을 우선합니다.
- 모든 폼에는 적절한 라벨, 검증 메시지, 키보드 접근성이 필요합니다.
- 아이콘 전용 버튼에는 접근 가능한 이름이 있어야 합니다.
- 모달, 드로어, 메뉴는 포커스가 올바르게 관리되어야 합니다.
- 차트는 장식용이 아니라, 실제 운영상 의미 있는 인사이트를 제공해야 합니다.

## 성능 기준
- 클라이언트 상태는 가능한 한 작게 유지합니다.
- 불필요한 재렌더링과 과도한 번들 크기를 피합니다.
- 메모이제이션은 실제로 필요할 때만 사용합니다.
- 데이터가 큰 테이블은 가상화도 고려합니다.
- 빈 화면보다는 점진적 렌더링과 스켈레톤/로딩 UI를 우선합니다.

## 테스트 및 검증
단순하지 않은 변경에는 테스트를 추가하거나 기존 테스트를 갱신합니다.

최소한 아래 항목은 핵심 테스트 범위에 포함되어야 합니다.

- 고객 검색 / 필터 동작
- 구독 상태 변경
- 환불 승인 / 반려 흐름
- 관리자 영역 라우트 보호 또는 권한 기반 접근 제어

작업을 마무리하기 전에, 가능한 경우 아래 검증을 실행합니다.

- `<pm> lint`
- `<pm> typecheck`
- `<pm> test`
- `<pm> build`

해당 명령이 없다면 적절히 추가하거나, 실행하지 못한 이유를 명확히 남깁니다.

## 계획 및 작업 방식
- 여러 화면, 여러 기능, 또는 아키텍처 결정이 포함된 작업은 먼저 `docs/execplans/<task-name>.md`를 작성합니다.
- 실행 계획 문서에는 아래 내용을 포함합니다.
  - 목표
  - 가정
  - 범위
  - 구현 단계
  - 리스크
  - 검증 방법
- 한 번에 크게 갈아엎는 방식보다, 작고 리뷰하기 쉬운 변경을 선호합니다.
- 사소한 모호함 때문에 멈추지 말고, 근거 있는 가정을 세운 뒤 문서에 남깁니다.

## 완료 기준
아래 조건을 모두 만족해야 작업이 완료된 것으로 봅니다.

- 요청한 동작이 실제로 작동한다.
- 변경된 화면에 빈 상태 / 에러 / 로딩 상태가 포함되어 있다.
- 타입 안정성이 유지된다.
- 관련 테스트와 검증이 갱신되고 실행되었다.
- 동작이나 아키텍처가 크게 바뀌었다면 README 또는 문서가 함께 업데이트되었다.

## 유지보수 규칙
같은 실수나 동일한 리뷰 코멘트가 두 번 이상 반복되면, 이후 작업에도 반영될 수 있도록 이 파일에 해당 내용을 추가합니다.

## Git 커밋 메시지 규칙
- 커밋 메시지는 기본적으로 **Conventional Commits** 형식을 사용합니다.
- 기본 형식은 `type(scope): summary` 입니다.
- `summary`는 명령형 현재 시제로 짧고 명확하게 작성합니다.
- 가능하면 50~72자 안쪽으로 유지하고, 무엇을 바꿨는지 중심으로 씁니다.
- 이 프로젝트에서 우선 사용하는 `type`은 다음과 같습니다.
  - `feat`: 기능 추가
  - `fix`: 버그 수정
  - `refactor`: 동작 변화 없는 구조 개선
  - `docs`: 문서 변경
  - `test`: 테스트 추가 또는 수정
  - `chore`: 설정, 의존성, 보일러플레이트 정리 등 기타 작업
  - `style`: 포맷팅 등 동작과 무관한 스타일 변경
  - `perf`: 성능 개선
- 예시
  - `feat(admin): add customer list skeleton`
  - `fix(payments): correct refund status mapping`
  - `chore(repo): remove starter boilerplate`
  - `docs(planning): update bootstrap plan`
